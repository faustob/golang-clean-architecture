# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity dependencies start -->
## Dependencies

Generated by HoundOS against `43aad26`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⚠ passed with warnings.
<details><summary>3 issues found and fixed during self-review</summary>

- general (`internal/config/gorm_metrics.go`): The task scope is REGISTRATION ONLY for the auto-instrumentation package github.com/XSAM/otelsql: add it to go.mod and register it in the SDK setup. Instead this diff hand-writes GORM callbacks, custom histograms and counters — explicitly forbidden because it covers fewer call sites, duplicates semconv metrics under hand-rolled names, and drifts as code changes.
- qr-metric-naming (`internal/config/gorm_metrics.go`): The error counter is named "db.client.operation.errors", which is not the platform-prescribed name. managedMetricNames prescribes "db.client.errors" for the DB error-rate SLI; the SLI-to-signal join is exact-name, so this drift reads as healthy-forever with no data behind the SLI.
- qr-preserves-behavior (`internal/config/gorm_metrics.go`): classifyGormError treats gorm.ErrRecordNotFound as an error and increments the error counter, but the DB is configured with IgnoreRecordNotFoundError: true, so record-not-found is not an application error; counting it inflates the DB Error Rate SLI.

</details>
**Outstanding (1):**
- ⚠ sli-not-implemented (`internal/config/gorm.go`:40): otelsql query-level metrics (db.client.operation.duration, db.client.errors) require wrapping the connection with otelsql instrumentation that records per-query stats; only RegisterDBStatsMetrics (connection-pool gauges) is registered, not the per-query metrics registration. The DB Query Latency (p95) and DB Error Rate SLIs depend on query-level operation duration/error signals that this diff does not enable. — _otelsql.Open already instruments query operations and emits db.client.operation.duration/errors — confirm the otelsql version records those semconv instruments; RegisterDBStatsMetrics alone only adds pool gauges._

## SLIs implemented
- **MySQL — DB Query Latency (p95)** (`dep-my-sql-dep-db-query-latency-p95`) — target < 100ms over 1-hour
- **MySQL — DB Error Rate** (`dep-my-sql-dep-db-error-rate`) — target < 0.5% over 1-hour

## Modified files
- `internal/config/gorm.go`
- `cmd/web/main.go`
- `go.mod`

## New files
- `internal/config/telemetry.go`

## Dependencies
- `github.com/XSAM/otelsql v0.35.0`
- `go.opentelemetry.io/otel v1.46.0`
- `go.opentelemetry.io/otel/metric v1.46.0`
- `go.opentelemetry.io/otel/sdk v1.46.0`
- `go.opentelemetry.io/otel/sdk/metric v1.46.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.46.0`
<!-- houndos:entity dependencies end -->

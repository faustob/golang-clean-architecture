# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity golang-clean-architecture start -->
## Golang-clean-architecture

Generated by HoundOS against `43aad26`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 1 generation pass — ⚠ passed with warnings.
**Outstanding (5):**
- ⚠ qr-otel-version-pin (`go.mod`:17): go.opentelemetry.io/otel is pinned at 1.36.0, but this job targets the OTel 1.46 line (default-latest). Different PRs against one repo pinning different OTel lines makes the dependency move for no stated reason. — _Pin the OTel dependencies to the 1.46 line, the version this job resolved from the knowledge base._
- ⚠ qr-metric-naming (`internal/delivery/http/middleware/otel_middleware.go`:33): The counter name "http.server.request.outcome.total" does not match the platform-prescribed name "http.server.request.outcomes" from managedMetricNames; the SLI-to-signal join is exact-name, so the availability/error-rate SLIs backed by this counter will not join to their signal. — _Rename the instrument to "http.server.request.outcomes" exactly as listed in managedMetricNames._
- ⚠ qr-metric-naming (`internal/delivery/http/middleware/otel_middleware.go`:42): The counter name "http.server.requests_by_tenant.total" does not match the platform-prescribed name "http.server.requests.by_tenant" from managedMetricNames; the per-tenant throughput SLI relies on the exact name. — _Rename the instrument to "http.server.requests.by_tenant" exactly as listed in managedMetricNames._
- ⚠ qr-metric-naming (`internal/delivery/http/middleware/otel_middleware.go`:73): Labelling http.server.requests.by_tenant with a raw X-Api-Key header value creates unbounded metric cardinality (one time series per API key), which the recipe explicitly warns against. — _Only record the tenant attribute from a bounded, known set of tenant identifiers, or drop this per-tenant instrument._
- ⚠ general (`go.mod`:13): go.mod's require block pins the OTel modules to v1.32.0 (in the changes hunk) but the reconciled content block lists v1.36.0 for the same modules; the inconsistent pins will not resolve cleanly. — _Pin every go.opentelemetry.io/otel module to a single consistent version across the go.mod content and the change hunk._

## SLIs implemented
- **Golang-clean-architecture — HTTP Service Availability** (`golang-clean-architecture-http-availability`) — target 99.95% over 30-day
- **Golang-clean-architecture — HTTP Response Time P95** (`golang-clean-architecture-http-latency-p95`) — target < 200ms over 1-hour
- **Golang-clean-architecture — HTTP Response Time P99** (`golang-clean-architecture-http-latency-p99`) — target < 750ms over 1-hour
- **Golang-clean-architecture — HTTP 5xx Error Rate** (`golang-clean-architecture-http-error-rate`) — target < 0.1% over 1-hour
- **Golang-clean-architecture — HTTP Request Throughput** (`golang-clean-architecture-http-request-rate`) — target baseline ± 30% over 5-minute

## Modified files
- `cmd/web/main.go`
- `internal/delivery/http/address_controller.go`
- `go.mod`

## New files
- `internal/config/otel.go`
- `internal/delivery/http/middleware/otel_middleware.go`

## Dependencies
- `go.opentelemetry.io/otel v1.32.0`
- `go.opentelemetry.io/otel/trace v1.32.0`
- `go.opentelemetry.io/otel/metric v1.32.0`
- `go.opentelemetry.io/otel/sdk v1.32.0`
- `go.opentelemetry.io/otel/sdk/metric v1.32.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.32.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.32.0`
- `github.com/gofiber/contrib/otelfiber/v2 v2.1.1`
<!-- houndos:entity golang-clean-architecture end -->

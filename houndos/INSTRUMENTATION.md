# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity asynchronous-processing-via-kafka-worker start -->
## Asynchronous Processing via Kafka Worker

Generated by HoundOS against `43aad26`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 1 generation pass — ⛔ unresolved blocking issues — opened as DRAFT.
**Outstanding (4):**
- ⚠ qr-go-span-context (`internal/delivery/http/address_controller.go`:54): The context returned by tracer.Start is discarded (`_, validationSpan := ...Start(flowCtx, ...)`) while `flowCtx` is still passed downstream — the calls that follow keep the OLD context, so their spans do not nest under this one and the trace breaks at this hop. — _Capture it and propagate: `flowCtx, validationSpan := tracer.Start(flowCtx, ...)` (or a new name), then pass THAT context to the calls below._
- ⛔ sli-not-implemented (`internal/delivery/http/address_controller.go`): All six targetSlis concern the "Asynchronous Processing via Kafka Worker" E2E flow, but the diff instruments only the synchronous HTTP AddressController.Create handler. No measurement point exists anywhere in the diff on the Kafka worker/consumer path that these SLIs measure — flow success, latency, validation-failure, throughput, freshness and abandonment for the async worker flow are all unimplemented. — _Instrument the Kafka consumer/worker where the async business flow is actually processed (record flow.entries/flow.outcomes/flow.duration on message consume→terminal), rather than only the HTTP address-create handler._
- ⛔ sli-not-implemented (`internal/telemetry/telemetry.go`): The freshness SLI (Flow Completion Freshness < 2 minutes) and abandonment SLI (Flow Abandonment Rate) have no dedicated measurement point: no instrument records completion recency, and no code path ever records an "abandoned" outcome (recordOutcome is only ever called with "success" or "failed"). — _Add measurement points for completion freshness and abandonment (e.g. record an "abandoned" outcome on the flow path and a freshness gauge/timestamp), on the async worker flow._
- ⚠ qr-metric-naming (`internal/telemetry/telemetry.go`): The managed metric name flow.entry_to_terminal.duration is prescribed for this job but is never created; the diff instead uses flow.duration for end-to-end duration. The SLI-to-signal join is exact-name, so the prescribed name is missing. — _Create the flow.entry_to_terminal.duration instrument as prescribed in managedMetricNames for the E2E latency/freshness SLIs._

## SLIs implemented
- **Asynchronous Processing via Kafka Worker — E2E Business Flow Success Rate** (`flow-async-worker-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Asynchronous Processing via Kafka Worker — E2E Business Flow Latency P95** (`flow-async-worker-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Asynchronous Processing via Kafka Worker — Flow Validation Failure Rate** (`flow-async-worker-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Asynchronous Processing via Kafka Worker — E2E Business Flow Throughput** (`flow-async-worker-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Asynchronous Processing via Kafka Worker — Flow Completion Freshness** (`flow-async-worker-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour
- **Asynchronous Processing via Kafka Worker — Flow Abandonment Rate** (`flow-async-worker-flow-e2e-flow-abandonment-rate`) — target < 10% over 7-day

## Modified files
- `cmd/web/main.go`
- `internal/delivery/http/address_controller.go`
- `go.mod`

## New files
- `internal/telemetry/telemetry.go`

## Dependencies
- `github.com/gofiber/contrib/otelfiber/v2 v2.1.1`
- `go.opentelemetry.io/otel v1.46.0`
- `go.opentelemetry.io/otel/sdk v1.46.0`
- `go.opentelemetry.io/otel/sdk/metric v1.46.0`
- `go.opentelemetry.io/otel/metric v1.46.0`
- `go.opentelemetry.io/otel/trace v1.46.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.46.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.46.0`
<!-- houndos:entity asynchronous-processing-via-kafka-worker end -->

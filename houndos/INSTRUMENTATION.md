# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity asynchronous-processing-via-kafka-worker start -->
## Asynchronous Processing via Kafka Worker

Generated by HoundOS against `43aad26`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 4 generation passes — ✓ clean — no issues found.
<details><summary>9 issues found and fixed during self-review</summary>

- qr-not-a-noop (`internal/config/otel.go`): InitTelemetry never registers a TextMapPropagator via otel.SetTextMapPropagator. The producer calls otel.GetTextMapPropagator().Inject and the consumer calls .Extract, but the global default propagator is a no-op, so no W3C traceparent header is ever written or read and the producer/consumer spans never link into one E2E trace.
- sli-fabricated-target (`cmd/web/main.go`): The web binary registers the SDK and holds the producer, but the producer's FlowEntries counter / flow.produce span only emit for the flows that actually call Send; the consumer-side flow.outcomes, flow.duration and flow.entry_to_terminal.duration are recorded in the worker process, which is a separate binary. The freshness/duration SLIs depend on entryTs stamped by the producer, but nothing in the diff shows the web-side use cases invoking Producer.Send, so it is not confirmed the flows are exercised.
- qr-preserves-behavior (`internal/delivery/messaging/consumer.go`): When entryTs is zero (no entry timestamp header, e.g. messages produced outside the instrumented producer) the consumer records neither FlowDuration nor FlowEntryToTerminalDuration, so the latency and freshness SLIs miss all such flow instances silently rather than counting them.
- qr-go-span-context (`internal/delivery/messaging/user_consumer.go`): The context returned by tracer.Start is discarded (`_, validateSpan := ...Start(ctx, ...)`) while `ctx` is still passed downstream — the calls that follow keep the OLD context, so their spans do not nest under this one and the trace breaks at this hop.
- qr-go-span-context (`internal/delivery/messaging/contact_consumer.go`): The context returned by tracer.Start is discarded (`_, validateSpan := ...Start(ctx, ...)`) while `ctx` is still passed downstream — the calls that follow keep the OLD context, so their spans do not nest under this one and the trace breaks at this hop.
- qr-go-span-context (`internal/delivery/messaging/address_consumer.go`): The context returned by tracer.Start is discarded (`_, validateSpan := ...Start(ctx, ...)`) while `ctx` is still passed downstream — the calls that follow keep the OLD context, so their spans do not nest under this one and the trace breaks at this hop.
- qr-preserves-behavior (`cmd/web/main.go`): cmd/web/main.go calls config.InitTelemetry but the Fiber HTTP server is never given any OTel middleware, so no server-side flow instrumentation runs on the web binary; however the SLIs target the worker/Kafka flow so this is not itself a defect. Omit.
- qr-preserves-behavior (`internal/delivery/messaging/consumer.go`): The consumer span is started from a fresh context.Background() extract on every message but MarkMessage is only called on success; the added telemetry does not alter this, so control flow is preserved. Omit.
- qr-not-a-noop (`internal/config/otel.go`): InitTelemetry never registers a TextMapPropagator via otel.SetTextMapPropagator. The producer calls otel.GetTextMapPropagator().Inject and the consumer calls Extract, but the default global propagator is a no-op, so trace context is never carried in Kafka headers and the consumer spans never roll into the producer's trace — the E2E flow correlation is dead.

</details>

## SLIs implemented
- **Asynchronous Processing via Kafka Worker — E2E Business Flow Success Rate** (`flow-async-worker-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Asynchronous Processing via Kafka Worker — E2E Business Flow Latency P95** (`flow-async-worker-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Asynchronous Processing via Kafka Worker — Flow Validation Failure Rate** (`flow-async-worker-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Asynchronous Processing via Kafka Worker — E2E Business Flow Throughput** (`flow-async-worker-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Asynchronous Processing via Kafka Worker — Flow Completion Freshness** (`flow-async-worker-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour
- **Asynchronous Processing via Kafka Worker — Flow Abandonment Rate** (`flow-async-worker-flow-e2e-flow-abandonment-rate`) — target < 10% over 7-day

## Modified files
- `internal/gateway/messaging/producer.go`
- `internal/delivery/messaging/consumer.go`
- `internal/delivery/messaging/user_consumer.go`
- `internal/delivery/messaging/contact_consumer.go`
- `internal/delivery/messaging/address_consumer.go`
- `cmd/worker/main.go`
- `cmd/web/main.go`
- `go.mod`

## New files
- `internal/telemetry/telemetry.go`
- `internal/config/otel.go`

## Dependencies
- `go.opentelemetry.io/otel v1.45.0`
- `go.opentelemetry.io/otel/metric v1.45.0`
- `go.opentelemetry.io/otel/sdk v1.45.0`
- `go.opentelemetry.io/otel/sdk/metric v1.45.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.45.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.45.0`
<!-- houndos:entity asynchronous-processing-via-kafka-worker end -->

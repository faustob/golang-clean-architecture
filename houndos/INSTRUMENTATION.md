# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity user-management-via-http-api start -->
## User Management via HTTP API

Generated by HoundOS against `43aad26`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⚠ passed with warnings.
<details><summary>2 issues found and fixed during self-review</summary>

- qr-endpoint-env-driven (`cmd/web/main.go`): Hardcoded OTLP endpoint "localhost:4317" — wrong in every real deployment.
- qr-export-pipeline (`cmd/web/main.go`): The OTLP exporter endpoint falls back to a hardcoded literal "localhost:4317" when the config key is empty, rather than being purely env-driven. Endpoints must be env-driven with no hardcoded host:port in code.

</details>
**Outstanding (4):**
- ⚠ qr-counter-timing (`internal/delivery/http/address_controller.go`): A metric is recorded from a defer/finally with a hardcoded success outcome ("success"), so it fires on error/early-return paths too — counting successes that didn't happen (and double-counting if the error branch also records). Record the SUCCESS outcome only in the success path, after the operation returns. — _Move the success/outcome record out of the defer/finally into the success path AFTER the operation completes; if you must defer, set the outcome from a VARIABLE that the error branch overrides — never a hardcoded "success"._
- ⚠ sli-not-implemented (`internal/delivery/http/address_controller.go`:1): The targetSlis concern "User Management via HTTP API" (the user CRUD flow), but the instrumentation was added to the AddressController rather than the user-facing controller. If the user flow lives in a separate controller (e.g. user_controller.go), the six user-CRUD SLIs have no measurement point on the path they claim to measure. — _Confirm the instrumentation targets the User Management HTTP endpoints; if the user flow is a different controller, add the flow instrumentation there._
- ⚠ qr-metric-naming (`internal/delivery/http/address_controller.go`:58): The histogram named flow.entry_to_terminal.duration is used to back the freshness SLI but is recorded with the same value as flow.duration (time.Since(start).Seconds() within a single request), so it does not actually measure wall-clock entry-to-terminal freshness distinctly; it is a duplicate of flow.duration under a different name. — _Either record a distinct freshness measurement or drop the duplicate; if the platform-prescribed name is required, record the intended metric semantics rather than reusing the request duration._
- ⚠ general (`go.mod`:18): go.mod's require block pins the OTel modules at v1.36.0 while the same block's change hunk and proposedDependencies specify v1.32.0; the final content and the diff hunk disagree on the version, which will not resolve consistently. — _Align the OTel module versions across go.mod and the imports (single pinned version consistent with the SDK/semconv path)._

## SLIs implemented
- **User Management via HTTP API — E2E Business Flow Success Rate** (`flow-user-crud-flow-e2e-flow-success`) — target 99.95% over 30-day
- **User Management via HTTP API — E2E Business Flow Latency P95** (`flow-user-crud-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **User Management via HTTP API — Flow Validation Failure Rate** (`flow-user-crud-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **User Management via HTTP API — E2E Business Flow Throughput** (`flow-user-crud-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **User Management via HTTP API — Flow Completion Freshness** (`flow-user-crud-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour
- **User Management via HTTP API — Flow Abandonment Rate** (`flow-user-crud-flow-e2e-flow-abandonment-rate`) — target < 10% over 7-day

## Modified files
- `cmd/web/main.go`
- `internal/delivery/http/address_controller.go`
- `go.mod`

## Dependencies
- `go.opentelemetry.io/otel v1.32.0`
- `go.opentelemetry.io/otel/metric v1.32.0`
- `go.opentelemetry.io/otel/sdk v1.32.0`
- `go.opentelemetry.io/otel/sdk/metric v1.32.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.32.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.32.0`
- `github.com/gofiber/contrib/otelfiber/v2 v2.1.1`
<!-- houndos:entity user-management-via-http-api end -->

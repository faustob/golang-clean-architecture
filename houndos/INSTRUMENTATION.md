# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity asynchronous-processing-via-kafka-worker start -->
## Asynchronous Processing via Kafka Worker

Generated by HoundOS against `43aad26`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 6 generation passes — ⚠ passed with warnings.
<details><summary>24 issues found and fixed during self-review</summary>

- sli-not-implemented (`internal/delivery/http/address_controller.go`): The controller's final code comments and the note file state the terminal flow metrics (FlowOutcomes success, FlowDuration, FlowEntryToTerminalDuration) are recorded by the Kafka worker in internal/worker/address_consumer.go, but that worker file is not present anywhere in this diff. On the HTTP success path no terminal outcome/duration/freshness metric is recorded, so the E2E flow success rate, latency P95, and freshness SLIs have no measurement point that ever fires for successful flows.
- sli-not-implemented (`internal/delivery/http/address_controller.go`): flow-async-worker-flow-e2e-flow-success has no measurement point on the success path in this diff: FlowOutcomes with outcome="success" is only recorded in a version of the code that the final hunk removes; the shipped Create() records no success outcome and the referenced worker file is absent, so success rate reads as never-succeeding.
- sli-not-implemented (`internal/delivery/http/address_controller.go`): flow-async-worker-flow-e2e-flow-latency (FlowDuration) is recorded only on error/validation-failure paths in the shipped controller; the success path records nothing and the worker file that supposedly records it is not in this diff, so the latency P95 SLI has no data for successful flows.
- sli-not-implemented (`internal/delivery/http/address_controller.go`): flow-async-worker-flow-e2e-flow-freshness (FlowEntryToTerminalDuration) has no recording point anywhere in the diff: the shipped controller removed all FlowEntryToTerminalDuration.Record calls and the worker file referenced for it is not present.
- qr-unused-instrument (`internal/telemetry/telemetry.go`): FlowEntryToTerminalDuration is created but recorded nowhere in the shipped diff (the controller's final hunk removes its record calls and the worker file is absent), so the instrument exists but nothing writes to it.
- qr-preserves-behavior (`internal/delivery/http/address_controller.go`): Create() sets request.FlowStartUnixNano, but CreateAddressRequest gaining that field is described only in a markdown note, not in a code change; if the field is not actually added to the model this file will not compile.
- sli-not-implemented (`internal/delivery/http/address_controller.go`): The freshness SLI (flow-async-worker-flow-e2e-flow-freshness) requires measuring entry-to-terminal time including the async Kafka hops, but FlowEntryToTerminalDuration is now recorded only synchronously in the HTTP handler (on the validation-fail and failure paths). On the success path the handler comments say the worker will record it, yet no worker file exists in the diff, so the terminal/freshness measurement point never runs.
- sli-not-implemented (`internal/delivery/http/address_controller.go`): The E2E flow success rate and abandonment SLIs depend on a TERMINAL flow outcome recorded by the async Kafka worker, but the success path only records FlowDuration and never records a terminal FlowOutcomes value; the worker consumer that the comments claim records the terminal success/failure outcome is not present anywhere in the diff, so success/abandonment can never be observed.
- qr-preserves-behavior (`internal/delivery/http/address_controller.go`): request.FlowStartUnixNano is assigned on model.CreateAddressRequest but no such field is shown to exist and no diff adds it; if the field is absent this will fail to compile.
- qr-metric-naming (`internal/telemetry/telemetry.go`): On the success path FlowOutcomes is never recorded (only FlowDuration is), and the comment references an "accepted" outcome value that the handler never actually emits — the intended synchronous checkpoint outcome is missing.
- general (`internal/delivery/http/address_controller.go`): request.FlowStartUnixNano is assigned on model.CreateAddressRequest, but no field of that name is shown to exist on the model; if the field was not added, this fails to compile.
- sli-not-implemented (`internal/delivery/http/address_controller.go`): The freshness SLI (flow-async-worker-flow-e2e-flow-freshness) and abandonment SLI are described in comments as being recorded asynchronously by the Kafka worker (internal/consumer), but no such recording exists anywhere in this diff — flow.entry_to_terminal.duration is only recorded on the synchronous validation-fail path, not on the async terminal completion the freshness SLI measures.
- sli-not-implemented (`internal/delivery/http/address_controller.go`): The abandonment SLI (flow-async-worker-flow-e2e-flow-abandonment-rate) has no measurement point: nothing records an 'abandoned' terminal outcome anywhere in the diff — flow.outcomes only records validation_failed/failure/success on the synchronous path, and the async worker that would detect abandonment is not instrumented.
- qr-preserves-behavior (`internal/delivery/http/address_controller.go`): On the successful path, flow.outcomes is no longer recorded: the final hunk removed the success outcome record and left only FlowDuration, so the success-rate SLI never observes success events and reads as if no flow ever succeeds.
- sli-not-implemented (`internal/delivery/http/address_controller.go`): The E2E flow success-rate SLI (flow-async-worker-flow-e2e-flow-success) has no success measurement point: the diff records validation_failed and failure outcomes but the success outcome record was removed, so flow.outcomes never emits an outcome=success value.
- sli-not-implemented (`internal/telemetry/telemetry.go`): The freshness SLI (flow-async-worker-flow-e2e-flow-freshness) is documented as being recorded by the Kafka worker at terminal processing time, but no worker/consumer file in this diff records FlowEntryToTerminalDuration. The only recorder in the diff (address_controller.go) records it at HTTP-accept time, and even that call was removed by the final hunk, so nothing emits the freshness measurement.
- sli-not-implemented (`internal/delivery/http/address_controller.go`): The freshness SLI depends on entry-to-terminal duration being recorded at the flow's terminal state by the Kafka worker, but FlowEntryToTerminalDuration.Record and FlowTerminalOutcomes.Add have no call site anywhere in the diff; the worker consumer that would compute freshness/terminal outcomes was never instrumented, so freshness is not implemented.
- qr-unused-instrument (`internal/telemetry/telemetry.go`): FlowTerminalOutcomes is created and exported but is never recorded anywhere in the diff (no worker call site), so the terminal-outcome / abandonment measurement it backs never emits.
- sli-not-implemented (`internal/delivery/http/address_controller.go`): The abandonment-rate SLI (flow-async-worker-flow-e2e-flow-abandonment-rate) relies on comparing FlowEntries against a terminal worker outcome, but the terminal worker instrumentation (FlowTerminalOutcomes) is never recorded, so abandoned flows can never be observed and the SLI is not implemented.
- qr-preserves-behavior (`internal/delivery/http/address_controller.go`): The Create handler sets request.FlowStartUnixNano on model.CreateAddressRequest, but no diff shows this field existing on the model; if the field is not present this is a compile-class change, and if the use case/Kafka publish path is unchanged the value is never propagated to the worker, so the intended freshness threading does not actually reach the worker.
- general (`internal/telemetry/telemetry.go`): resource.Merge requires both resources to use the same schema URL; resource.Default() carries a schema URL while resource.NewSchemaless(...) has none, which returns a schema-conflict error at runtime and makes Setup fail before providers are registered.
- sli-fabricated-target (`internal/delivery/http/address_controller.go`): The flow instrumentation is entirely synchronous: FlowOutcomes/FlowDuration/FlowEntryToTerminalDuration all record their terminal values within the HTTP Create handler at the moment the use case returns, before any Kafka publish or worker processing completes. The SLIs claim to measure an async Kafka worker E2E flow (success/latency/freshness/abandonment) but the metrics fire on the synchronous request boundary, so freshness and abandonment can never be observed and latency excludes all async hops.
- sli-not-implemented (`internal/delivery/http/address_controller.go`): Flow Completion Freshness (flow-async-worker-flow-e2e-flow-freshness) has no genuine measurement point: FlowEntryToTerminalDuration is recorded synchronously in the HTTP handler at request completion, so it measures request latency, not the entry-to-async-terminal freshness the SLI defines. No worker-side terminal measurement exists in the diff.
- sli-not-implemented (`internal/delivery/http/address_controller.go`): Flow Abandonment Rate (flow-async-worker-flow-e2e-flow-abandonment-rate) is not implemented: every entry counted by FlowEntries reaches a terminal FlowOutcomes record within the same synchronous handler, so an abandoned flow (entered but never terminally processed by the worker) can never be represented.

</details>
**Outstanding (3):**
- ⚠ sli-fabricated-target (`internal/telemetry/telemetry.go`:40): FlowTerminalOutcomes (flow.terminal.outcomes) is declared and created for the Kafka worker's terminal-outcome measurement but nothing in this diff records to it — the freshness/abandonment SLIs that depend on the worker-side terminal are backed only by the synchronous HTTP-boundary metrics, which measure the request handler, not the async worker completion.
- ⚠ sli-not-implemented (`internal/delivery/http/address_controller.go`:40): The flow-completion-freshness and flow-abandonment SLIs for the async Kafka worker are measured only at the synchronous HTTP handler boundary (FlowEntryToTerminalDuration is recorded via time.Since(flowStart) at handler return, before the async Kafka hop completes), so they measure request latency rather than end-to-end async completion; no worker-side terminal measurement is emitted.
- ⚠ general (`internal/delivery/http/address_controller.go`:40): The Create handler instruments only the address.create path (a single controller), but the E2E flow SLIs target the Asynchronous Processing via Kafka Worker flow; there is no measurement point in the Kafka consumer/worker path, so throughput and success are recorded at the HTTP boundary rather than at the actual async terminal.

## SLIs implemented
- **Asynchronous Processing via Kafka Worker — E2E Business Flow Success Rate** (`flow-async-worker-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Asynchronous Processing via Kafka Worker — E2E Business Flow Latency P95** (`flow-async-worker-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Asynchronous Processing via Kafka Worker — Flow Validation Failure Rate** (`flow-async-worker-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Asynchronous Processing via Kafka Worker — E2E Business Flow Throughput** (`flow-async-worker-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Asynchronous Processing via Kafka Worker — Flow Completion Freshness** (`flow-async-worker-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour
- **Asynchronous Processing via Kafka Worker — Flow Abandonment Rate** (`flow-async-worker-flow-e2e-flow-abandonment-rate`) — target < 10% over 7-day

## Modified files
- `cmd/web/main.go`
- `internal/delivery/http/address_controller.go`
- `go.mod`

## New files
- `internal/telemetry/telemetry.go`

## Dependencies
- `go.opentelemetry.io/otel v1.46.0`
- `go.opentelemetry.io/otel/sdk v1.46.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.46.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.46.0`
<!-- houndos:entity asynchronous-processing-via-kafka-worker end -->

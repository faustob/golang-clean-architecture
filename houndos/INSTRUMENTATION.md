# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity dependencies start -->
## Dependencies

Generated by HoundOS against `43aad26`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ✓ clean — no issues found.
<details><summary>2 issues found and fixed during self-review</summary>

- qr-unwired-telemetry (`internal/telemetry/db.go`): The task was registration-only for the auto-instrumentation package github.com/XSAM/otelsql, but this diff instead hand-writes a histogram and manually instruments the GORM repository call sites. The prescribed otelsql instrumentation is never added to go.mod nor registered, so the intended semconv coverage of every DB call site is not delivered.
- qr-preserves-behavior (`internal/repository/repository.go`): RecordDBOperation reads db.Statement.Context directly; for a GORM *gorm.DB whose Statement is nil (a freshly chained session that has not run a callback yet) this dereferences a nil pointer and panics, changing behavior of the repository methods that previously just returned an error.

</details>

## SLIs implemented
- **MySQL — DB Query Latency (p95)** (`dep-my-sql-dep-db-query-latency-p95`) — target < 100ms over 1-hour
- **MySQL — DB Error Rate** (`dep-my-sql-dep-db-error-rate`) — target < 0.5% over 1-hour

## Modified files
- `go.mod`
- `cmd/web/main.go`
- `internal/config/gorm.go`

## New files
- `internal/config/otel.go`

## Dependencies
- `github.com/XSAM/otelsql v0.35.0`
- `go.opentelemetry.io/otel v1.45.0`
- `go.opentelemetry.io/otel/sdk v1.45.0`
- `go.opentelemetry.io/otel/sdk/metric v1.45.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.45.0`
<!-- houndos:entity dependencies end -->

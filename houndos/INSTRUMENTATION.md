# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity dependencies start -->
## Dependencies

Generated by HoundOS against `43aad26`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⚠ passed with warnings.
<details><summary>3 issues found and fixed during self-review</summary>

- general: The task scope was REGISTRATION ONLY for the auto-instrumentation package github.com/XSAM/otelsql: add it to go.mod and register it in the SDK setup. Instead the diff hand-writes GORM callbacks around DB operations and never adds or uses otelsql, contradicting the dependency-instrumentation directive that forbids hand-written instrumentation.
- qr-preserves-behavior: The GORM 'after' callbacks record telemetry using context.Background() rather than the transaction's context (tx.Statement.Context), so spans/metrics are not correlated with the incoming request context and any span-based propagation is broken; more importantly this is a per-operation allocation on the DB hot path added by the instrumentation. This detaches recorded metrics from request-scoped context.
- qr-metric-naming: The diff introduces the counter name "db.client.operations" (in managedMetricNames) but the DB Error Rate SLI needs an error signal; managedMetricNames also lists db.client.errors, which is never created, so the error-rate SLI is only derivable via the error.type attribute on db.client.operations rather than the prescribed db.client.errors metric.

</details>
**Outstanding (2):**
- ⚠ general (`go.mod`:13): The go.mod require block pins the OTel packages at v1.31.0 (per the changes hunk 'with' text) while the file content lists them at v1.36.0, and the authoritative otelTarget is 1.46 — the version pins are inconsistent with each other and with the stated target. The compile gate will surface the mismatch. — _Pin all OTel SDK/exporter modules to a single consistent version aligned with the otelTarget (1.46-era) release._
- ⚠ qr-metric-naming (`internal/config/gorm.go`:33): otelsql.WithAttributes sets db.system.name and db.namespace, but the semconv v1.26.0 attribute keys are db.system and db.namespace; db.system.name is a later-renamed key that will not match the SLI's expected attribute semantics for the pinned semconv version. — _Use the attribute constants from the semconv package version in use (semconv.DBSystemMySQL / semconv.DBNamespace) rather than hardcoded string keys._

## SLIs implemented
- **MySQL — DB Query Latency (p95)** (`dep-my-sql-dep-db-query-latency-p95`) — target < 100ms over 1-hour
- **MySQL — DB Error Rate** (`dep-my-sql-dep-db-error-rate`) — target < 0.5% over 1-hour

## Modified files
- `go.mod`
- `internal/config/gorm.go`
- `cmd/web/main.go`

## New files
- `internal/config/telemetry.go`

## Dependencies
- `github.com/XSAM/otelsql v0.35.0`
- `go.opentelemetry.io/otel v1.31.0`
- `go.opentelemetry.io/otel/sdk v1.31.0`
- `go.opentelemetry.io/otel/sdk/metric v1.31.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.31.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.31.0`
<!-- houndos:entity dependencies end -->

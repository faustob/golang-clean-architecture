# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity golang-clean-architecture start -->
## Golang-clean-architecture

Generated by HoundOS against `43aad26`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⚠ passed with warnings.
<details><summary>3 issues found and fixed during self-review</summary>

- general (`cmd/web/main.go`): main() redeclares err with `err := app.Listen(...)` after err was already declared by the earlier `otelShutdown, err := telemetry.InitOTel(...)`, causing a `no new variables on left side of :=` compile error.
- qr-instrument-binding-order (`internal/telemetry/otel.go`): The package-level Meter is nil until InitOTel runs, but the middleware package's init() calls telemetry.Meter.Float64Histogram at package-initialization time; since Go runs imported-package init() before main() executes InitOTel, telemetry.Meter is still the zero (nil) metric.Meter and the instrument creation will nil-panic before registration.
- qr-metric-naming (`internal/delivery/http/middleware/otel_request_metrics.go`): The new counter names `http.server.request.outcome.total` and `http.server.request.by_tenant.total` do not match the platform-prescribed managed names `http.server.request.outcomes` and `http.server.requests.by_tenant`; the exact-name SLI join will fail to find data behind these SLIs.

</details>
**Outstanding (3):**
- ⚠ qr-otel-version-pin (`go.mod`:16): go.opentelemetry.io/otel is pinned at 1.38.0, but this job targets the OTel 1.46 line (default-latest). Different PRs against one repo pinning different OTel lines makes the dependency move for no stated reason. — _Pin the OTel dependencies to the 1.46 line, the version this job resolved from the knowledge base._
- ⚠ qr-metric-naming (`internal/delivery/http/middleware/otel_request_metrics.go`:25): The new histogram carries a duration unit of seconds but is named "http.server.request.duration" without a unit suffix while the managed-name corpus lists a sibling "http.server.request.duration_count"; the bare name is on the managedMetricNames list so this is exempt. — _No change — name is prescribed._
- ⚠ qr-metric-naming (`internal/delivery/http/middleware/otel_request_metrics.go`:52): The tenant attribute is derived from the X-API-Key request header (attribute "tenant" on http.server.requests.by_tenant), which is unbounded, attacker-controllable cardinality and will explode the metric time series. — _Map the API key to a bounded, validated tenant id (or drop the per-key label) before using it as a metric attribute._

## SLIs implemented
- **Golang-clean-architecture — HTTP Service Availability** (`golang-clean-architecture-http-availability`) — target 99.95% over 30-day
- **Golang-clean-architecture — HTTP Response Time P95** (`golang-clean-architecture-http-latency-p95`) — target < 200ms over 1-hour
- **Golang-clean-architecture — HTTP Response Time P99** (`golang-clean-architecture-http-latency-p99`) — target < 750ms over 1-hour
- **Golang-clean-architecture — HTTP 5xx Error Rate** (`golang-clean-architecture-http-error-rate`) — target < 0.1% over 1-hour
- **Golang-clean-architecture — HTTP Request Throughput** (`golang-clean-architecture-http-request-rate`) — target baseline ± 30% over 5-minute

## Modified files
- `cmd/web/main.go`
- `go.mod`

## New files
- `internal/telemetry/otel.go`
- `internal/delivery/http/middleware/otel_request_metrics.go`

## Dependencies
- `go.opentelemetry.io/otel v1.38.0`
- `go.opentelemetry.io/otel/sdk v1.38.0`
- `go.opentelemetry.io/otel/sdk/metric v1.38.0`
- `go.opentelemetry.io/otel/metric v1.38.0`
- `go.opentelemetry.io/otel/trace v1.38.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.38.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.38.0`
- `github.com/gofiber/contrib/otelfiber/v2 v2.2.0`
- `go.opentelemetry.io/otel v1.46.0`
- `go.opentelemetry.io/otel/sdk v1.46.0`
- `go.opentelemetry.io/otel/sdk/metric v1.46.0`
- `go.opentelemetry.io/otel/metric v1.46.0`
- `go.opentelemetry.io/otel/trace v1.46.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.46.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.46.0`
<!-- houndos:entity golang-clean-architecture end -->

# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity golang-clean-architecture start -->
## Golang-clean-architecture

Generated by HoundOS against `43aad26`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⚠ passed with warnings.
<details><summary>4 issues found and fixed during self-review</summary>

- qr-not-a-noop (`cmd/web/main.go`): initOtelSDK calls middleware.SetHTTPServerRequestDuration(...) but the file never imports the middleware package, so the function references an undeclared symbol and will not compile.
- qr-unused-instrument (`cmd/web/main.go`): The http.server.request.duration histogram is created and handed to middleware.SetHTTPServerRequestDuration, but no file in this diff records to it (no middleware calling Record is present); the instrument exists but nothing observes a duration, so the histogram emits no data.
- sli-not-implemented (`cmd/web/main.go`): The HTTP request throughput / error-rate / availability SLIs rely on a request-count and status-code labeled metric, but the only custom metric created (a duration histogram) is never recorded, and otelfiber traces do not by themselves back these count-based metric SLIs; no measurement point delivers request rate or 5xx error rate.
- sli-not-implemented (`cmd/web/main.go`): The five HTTP SLIs (availability, P95/P99 latency, 5xx error rate, request throughput) are all metric-based, but the only metrics wiring is otelfiber.Middleware(), which emits Fiber's own HTTP server metrics/spans; no instrument is created for the platform-prescribed http.server.request.duration and no measurement point in this diff records it. The span-event-based slowRequestThreshold annotations in the controllers do not produce any of these histogram/counter metrics.

</details>
**Outstanding (3):**
- ⚠ qr-duplicate-metric (`internal/delivery/http/address_controller.go`:22): Metric name "http.server.request.duration" is created 2 times in this change (cmd/web/main.go:63, internal/delivery/http/address_controller.go:22). Duplicate instrument registrations under one name produce merged/duplicate streams (double-counting), or an export conflict if their kind/unit differ. — _Create the "http.server.request.duration" instrument ONCE (a single shared meter/instrument reused across the call sites) and record to it from each place, rather than registering it in multiple files._
- ⚠ general (`cmd/web/main.go`:58): initOtelSDK assigns to an undeclared package-level variable `httpRequestDuration` (`httpRequestDuration, err = ...`), which does not exist anywhere in the diff; the histogram declared in address_controller.go is named `httpServerRequestDuration`. This will not compile. — _Remove the redundant httpRequestDuration creation from main.go (address_controller.go already creates the histogram it uses), or reference the correct variable._
- ⚠ general (`cmd/web/main.go`:15): main.go imports `go.opentelemetry.io/otel/metric` implicitly via `metric.WithUnit`/`metric.WithDescription` in the httpRequestDuration block but only imports `go.opentelemetry.io/otel/sdk/metric` aliased as `metric`; the sdk metric package does not provide WithUnit/WithDescription (those live in the API metric package), so these calls will not compile. — _Import the API metric package (go.opentelemetry.io/otel/metric) under a distinct alias for the instrument options, and drop the duplicate histogram-creation block._

## SLIs implemented
- **Golang-clean-architecture — HTTP Service Availability** (`golang-clean-architecture-http-availability`) — target 99.95% over 30-day
- **Golang-clean-architecture — HTTP Response Time P95** (`golang-clean-architecture-http-latency-p95`) — target < 200ms over 1-hour
- **Golang-clean-architecture — HTTP Response Time P99** (`golang-clean-architecture-http-latency-p99`) — target < 750ms over 1-hour
- **Golang-clean-architecture — HTTP 5xx Error Rate** (`golang-clean-architecture-http-error-rate`) — target < 0.1% over 1-hour
- **Golang-clean-architecture — HTTP Request Throughput** (`golang-clean-architecture-http-request-rate`) — target baseline ± 30% over 5-minute

## Modified files
- `cmd/web/main.go`
- `internal/delivery/http/address_controller.go`
- `go.mod`

## Dependencies
- `go.opentelemetry.io/otel v1.45.0`
- `go.opentelemetry.io/otel/sdk v1.45.0`
- `go.opentelemetry.io/otel/sdk/metric v1.45.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.45.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.45.0`
- `github.com/gofiber/contrib/otelfiber/v2 v2.1.1`
- `go.opentelemetry.io/otel/metric v1.45.0`
<!-- houndos:entity golang-clean-architecture end -->

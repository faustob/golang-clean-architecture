# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity golang-clean-architecture start -->
## Golang-clean-architecture

Generated by HoundOS against `43aad26`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 4 generation passes — ⚠ passed with warnings.
<details><summary>7 issues found and fixed during self-review</summary>

- qr-otel-api-signatures (`internal/delivery/http/middleware/otel_middleware.go`): The instrument constructor returns (instrument, error) — the error is discarded here, so a construction failure yields a nil instrument that panics on first use.
- qr-otel-version-pin (`go.mod`): go.opentelemetry.io/otel is pinned at 1.36.0, but this job targets the OTel 1.46 line (default-latest). Different PRs against one repo pinning different OTel lines makes the dependency move for no stated reason.
- qr-metric-naming (`internal/delivery/http/middleware/otel_middleware.go`): The new counter is named "http.server.requests", which is not in the managedMetricNames list; the platform prescribes "http.server.request.outcomes" and "http.server.requests.by_tenant" for outcome and per-tenant throughput, so this drifting name will not join to the SLI signal.
- qr-metric-naming (`internal/config/telemetry.go`): go.mod pins the OTel modules at v1.36.0 while proposedDependencies and the stated otelTarget are v1.32.0/1.46; the semconv import path go.opentelemetry.io/otel/semconv/v1.26.0 and version pins are internally inconsistent with the target.
- qr-metric-naming (`internal/config/telemetry.go`): The semconv import path go.opentelemetry.io/otel/semconv/v1.34.0 does not match the SDK v1.46.0 target's guidance example (v1.26.0), and a nonexistent semconv version package will not compile.
- qr-unwired-telemetry (`internal/delivery/http/middleware/otel_middleware.go`): c.Route().Path is read after c.Next() returns, but Fiber's route pattern may be empty for unmatched routes; the fallback to "unknown" is handled, so this is minor, but route templating relies on Fiber populating Route() which is available post-routing.
- qr-unwired-telemetry (`internal/delivery/http/middleware/otel_middleware.go`): The custom middleware records duration/outcome/tenant metrics but reads the current span from c.UserContext(), while otelfiber stores its span in the Fiber context via c.Locals; the span annotations (error.type, slow.request event) attach to a non-recording no-op span. The metric recording itself is fine, so this only affects the span-enrichment branch.

</details>
**Outstanding (1):**
- ⚠ general (`internal/config/fiber.go`:15): Both otelfiber.Middleware() (registered in fiber.go via app.Use) and NewOtelMiddleware() (registered in route.go) start a server span for every request, producing two overlapping HTTP server spans per request. Consider whether both span sources are intended. — _Keep otelfiber for the server span and have NewOtelMiddleware only record metrics/annotations without starting its own duplicate top-level span, or drop one of the two._

## SLIs implemented
- **Golang-clean-architecture — HTTP Service Availability** (`golang-clean-architecture-http-availability`) — target 99.95% over 30-day
- **Golang-clean-architecture — HTTP Response Time P95** (`golang-clean-architecture-http-latency-p95`) — target < 200ms over 1-hour
- **Golang-clean-architecture — HTTP Response Time P99** (`golang-clean-architecture-http-latency-p99`) — target < 750ms over 1-hour
- **Golang-clean-architecture — HTTP 5xx Error Rate** (`golang-clean-architecture-http-error-rate`) — target < 0.1% over 1-hour
- **Golang-clean-architecture — HTTP Request Throughput** (`golang-clean-architecture-http-request-rate`) — target baseline ± 30% over 5-minute

## Modified files
- `go.mod`
- `cmd/web/main.go`
- `internal/config/fiber.go`
- `internal/delivery/http/route/route.go`

## New files
- `internal/config/telemetry.go`
- `internal/delivery/http/middleware/otel_middleware.go`

## Dependencies
- `go.opentelemetry.io/otel v1.46.0`
- `go.opentelemetry.io/otel/sdk v1.46.0`
- `go.opentelemetry.io/otel/sdk/metric v1.46.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.46.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.46.0`
- `github.com/gofiber/contrib/otelfiber/v2 v2.1.1`
<!-- houndos:entity golang-clean-architecture end -->

# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity user-management-via-http-api start -->
## User Management via HTTP API

Generated by HoundOS against `43aad26`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⚠ passed with warnings.
<details><summary>2 issues found and fixed during self-review</summary>

- qr-go-span-context (`internal/telemetry/flow.go`): The context returned by tracer.Start is discarded (`_, span := ...Start(ctx, ...)`) while `ctx` is still passed downstream — the calls that follow keep the OLD context, so their spans do not nest under this one and the trace breaks at this hop.
- qr-not-a-noop (`go.mod`): go.mod's require block pins the otel modules twice: the pre-existing block declares v1.36.0 while the added hunk declares the same seven modules at v1.31.0, producing duplicate require entries for identical module paths, which is not a valid go.mod.

</details>
**Outstanding (2):**
- ⚠ qr-otel-version-pin (`go.mod`:16): go.opentelemetry.io/otel is pinned at 1.36.0, but this job targets the OTel 1.46 line (default-latest). Different PRs against one repo pinning different OTel lines makes the dependency move for no stated reason. — _Pin the OTel dependencies to the 1.46 line, the version this job resolved from the knowledge base._
- ⚠ sli-not-implemented (`internal/telemetry/flow.go`): The flow abandonment-rate SLI (flow-user-crud-flow-e2e-flow-abandonment-rate) has no measurement point. The middleware always runs both entry and outcome recording in the same request lifecycle, so there is no instrument capturing entries that never reach a terminal outcome (abandonment). — _Add an abandonment signal — e.g. compare flow.entries against flow.outcomes, or record an explicit abandoned outcome when the handler chain does not complete a terminal state._

## SLIs implemented
- **User Management via HTTP API — E2E Business Flow Success Rate** (`flow-user-crud-flow-e2e-flow-success`) — target 99.95% over 30-day
- **User Management via HTTP API — E2E Business Flow Latency P95** (`flow-user-crud-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **User Management via HTTP API — Flow Validation Failure Rate** (`flow-user-crud-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **User Management via HTTP API — E2E Business Flow Throughput** (`flow-user-crud-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **User Management via HTTP API — Flow Completion Freshness** (`flow-user-crud-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour
- **User Management via HTTP API — Flow Abandonment Rate** (`flow-user-crud-flow-e2e-flow-abandonment-rate`) — target < 10% over 7-day

## Modified files
- `go.mod`
- `cmd/web/main.go`
- `internal/delivery/http/route/route.go`
- `internal/usecase/user_usecase.go`

## New files
- `internal/telemetry/flow.go`
- `internal/delivery/http/middleware/flow_telemetry_middleware.go`
- `internal/config/telemetry.go`

## Dependencies
- `go.opentelemetry.io/otel v1.36.0`
- `go.opentelemetry.io/otel/trace v1.36.0`
- `go.opentelemetry.io/otel/metric v1.36.0`
- `go.opentelemetry.io/otel/sdk v1.36.0`
- `go.opentelemetry.io/otel/sdk/metric v1.36.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.36.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.36.0`
<!-- houndos:entity user-management-via-http-api end -->

# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity golang-clean-architecture start -->
## Golang-clean-architecture

Generated by HoundOS against `43aad26`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 6 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
<details><summary>14 issues found and fixed during self-review</summary>

- qr-preserves-behavior (`internal/telemetry/middleware.go`): The middleware calls span.SetStatus/SetAttributes/AddEvent on the span from ctx.UserContext(); if otelfiber stored the span elsewhere or the context has no active span, SpanFromContext returns a no-op span (safe), but the slow-request event and error attributes will silently attach to nothing when no span is present. This is not a crash but the span-event telemetry can be dropped.
- qr-metric-naming (`internal/telemetry/middleware.go`): The counter uses attribute key 'outcome' (a bare word not namespaced) alongside semconv-style http.* keys; this is an ad-hoc attribute name rather than a conforming one, though the metric name http.server.request.outcomes itself is in managedMetricNames and is exempt.
- edit-not-applied (`go.mod`): An intended edit to go.mod could not be applied (its target text wasn't found verbatim, wasn't unique, or overlapped another edit), so the file was left unchanged to avoid a half-applied result: rejected: non-additive manifest edit (would drop "go.opentelemetry.io/otel v1.36.0") — re-emit with newString = the entire oldString plus the additions. Pin OTel dependencies to the 1.46 line as targeted by this job
- qr-preserves-behavior (`internal/telemetry/middleware.go`): The middleware calls ctx.Next() and captures its error, but when herr != nil it still returns herr — however it also sets the span status and records after Next; this preserves control flow. Not a defect.
- edit-not-applied (`go.mod`): An intended edit to go.mod could not be applied (its target text wasn't found verbatim, wasn't unique, or overlapped another edit), so the file was left unchanged to avoid a half-applied result: rejected: non-additive manifest edit (would drop "go.opentelemetry.io/otel v1.36.0") — re-emit with newString = the entire oldString plus the additions. Pin OTel dependencies to the 1.46 line to match the target version.
- qr-unwired-telemetry (`internal/telemetry/middleware.go`): main.go registers requestOutcomeMiddlewareHandler (returned from NewRequestOutcomeMiddleware), but requestOutcomeCounter is only assigned inside NewRequestOutcomeMiddleware. That is fine — however the exported RequestOutcomeMiddleware function referenced by `var _ = RequestOutcomeMiddleware` does not exist; only requestOutcomeMiddlewareHandler and NewRequestOutcomeMiddleware are defined.
- qr-metric-naming (`internal/telemetry/middleware.go`): The counter is created with unit "{request}" and name http.server.request.outcomes but the SLO templates in managedMetricNames list http.server.request.outcomes as the canonical name — this is prescribed and correct, so no naming action needed; however the span-event attribute http.server.request.duration_seconds duplicates a managed metric concept as an ad-hoc attribute name.
- edit-not-applied (`go.mod`): An intended edit to go.mod could not be applied (its target text wasn't found verbatim, wasn't unique, or overlapped another edit), so the file was left unchanged to avoid a half-applied result: rejected: non-additive manifest edit (would drop "go.opentelemetry.io/otel v1.36.0") — re-emit with newString = the entire oldString plus the additions. Pin OTel Go dependencies to the 1.46 line as required by the job target.
- qr-preserves-behavior (`internal/telemetry/middleware.go`): RequestOutcomeMiddleware calls span.AddEvent/SetAttributes on the span returned by trace.SpanFromContext(ctx.UserContext()). Fiber's ctx.UserContext() does not carry the otelfiber span unless otelfiber propagates it there, and when no span is in context SpanFromContext returns a non-recording no-op span — the slow-request events and error.type attributes are silently dropped, so the P99 slow-request signal never records.
- general (`go.mod`): go.mod pins the OTel modules at v1.36.0 in the final content while the changes hunk adds v1.32.0 lines, producing duplicate require entries for the same modules (v1.32.0 and v1.36.0). The setup target is v1.46; the pins contradict both the stated target and each other.
- qr-metric-naming (`internal/telemetry/middleware.go`): The span-event attribute name http.server.request.duration_seconds encodes the unit as a fake namespace segment joined by underscore, which conflicts with the semconv duration style (http.server.request.duration) used elsewhere in the managed name set.
- qr-unwired-telemetry (`cmd/web/main.go`): telemetry.RequestOutcomeMiddleware is registered via app.Use BEFORE config.Bootstrap runs, but the recovery middleware (Fiber's recover) is set up inside Bootstrap/NewFiber ordering — verify the outcome middleware sits after Recover so panics are still recovered before the outcome is observed.
- qr-metric-naming (`internal/telemetry/middleware.go`): The span-event attribute name http.server.request.duration_seconds uses a duration suffix style inconsistent with the managed metric names, but as a span event attribute (not a metric) this is a minor naming note only.
- edit-not-applied (`go.mod`): An intended edit to go.mod could not be applied (its target text wasn't found verbatim, wasn't unique, or overlapped another edit), so the file was left unchanged to avoid a half-applied result: rejected: non-additive manifest edit (would drop "go.opentelemetry.io/otel v1.36.0") — re-emit with newString = the entire oldString plus the additions. Pin OTel dependencies to the 1.46 line to match the target OTel version.

</details>
**Outstanding (3):**
- ⚠ qr-otel-version-pin (`go.mod`:17): go.opentelemetry.io/otel is pinned at 1.36.0, but this job targets the OTel 1.46 line (default-latest). Different PRs against one repo pinning different OTel lines makes the dependency move for no stated reason. — _Pin the OTel dependencies to the 1.46 line, the version this job resolved from the knowledge base._
- ⚠ qr-metric-naming (`internal/telemetry/middleware.go`:29): The new counter is created as "http.server.request.outcome" (singular), but the platform's managed metric name for this concept is "http.server.request.outcomes" (plural). The exact-name SLI-to-signal join means this drift reads as healthy-forever with no data behind the availability/error-rate SLIs. — _Rename the counter to the prescribed managed name "http.server.request.outcomes"._
- ⛔ qr-preserves-behavior (`internal/telemetry/middleware.go`:30): init() panics on instrument-creation error (panic(err)), which aborts the whole process at package load time if instrument construction fails; the wiring recipe requires capturing and handling the error, not crashing the app. — _Return/log the error instead of panicking, or create the instrument inside a constructor that returns (fiber.Handler, error) and handle it in main()._

## SLIs implemented
- **Golang-clean-architecture — HTTP Service Availability** (`golang-clean-architecture-http-availability`) — target 99.95% over 30-day
- **Golang-clean-architecture — HTTP Response Time P95** (`golang-clean-architecture-http-latency-p95`) — target < 200ms over 1-hour
- **Golang-clean-architecture — HTTP Response Time P99** (`golang-clean-architecture-http-latency-p99`) — target < 750ms over 1-hour
- **Golang-clean-architecture — HTTP 5xx Error Rate** (`golang-clean-architecture-http-error-rate`) — target < 0.1% over 1-hour
- **Golang-clean-architecture — HTTP Request Throughput** (`golang-clean-architecture-http-request-rate`) — target baseline ± 30% over 5-minute

## Modified files
- `cmd/web/main.go`
- `go.mod`

## New files
- `internal/telemetry/otel.go`
- `internal/telemetry/middleware.go`

## Dependencies
- `go.opentelemetry.io/otel v1.32.0`
- `go.opentelemetry.io/otel/trace v1.32.0`
- `go.opentelemetry.io/otel/metric v1.32.0`
- `go.opentelemetry.io/otel/sdk v1.32.0`
- `go.opentelemetry.io/otel/sdk/metric v1.32.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.32.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.32.0`
- `github.com/gofiber/contrib/otelfiber/v2 v2.1.1`
<!-- houndos:entity golang-clean-architecture end -->
